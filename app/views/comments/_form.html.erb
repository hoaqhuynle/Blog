<%= form_for(@comment, :remote => true) do |form| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :username, id: :comment_username, class: 'form-control', value: current_user.email %>
  </div>

  <div class="field form-group">
    <%= form.text_area :body, id: :comment_body, class: 'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.hidden_field :article_id, id: :comment_article_id %>
  </div>

    <div class="field form-group">
    <%= form.hidden_field :user_id, value: current_user.id %>
  </div>

  <div class="actions">
    <p><%= form.submit class: 'btn btn-primary add-comment' %></p>
  </div>
<% end %>
